# RP2040 SPI bring-up — 2026-09-07

## Артефакт

- Файл: [gb_cart_rp2040.uf2](gb_cart_rp2040.uf2), 80896 байт.
- SHA-256: `a2cba0aedb337bc30e8ba97e367cef52164f00ab2bb95addc02bc0a7c4e27a28`.
- Скопирован без пересборки из `firmware/rp2040/spi_bringup/build/gb_cart_rp2040.uf2`.
- Время изменения исходного файла: 2026-09-07 20:24:47 (Asia/Nicosia).
- Конфигурация: `spi_bringup`, Waveshare RP2040-Zero, протокол FPGA 1.0.
- Текущая ревизия репозитория при сохранении: `4c0a7b8dd871d8f4c41193bbbc22fd10b25a9fbf`;
  рабочее дерево до обновления документации и выпуска было чистым.
  Ревизия непосредственно в момент сборки не записана в артефакте.

## Инструменты

По сохранённым файлам каталога сборки:

- Pico SDK 2.3.1 (`generated/pico_base/pico/version.h`).
- ARM GCC 9.2.1 (`CMakeFiles/*/CMakeCCompiler.cmake`).
- Release, `PICO_BOARD=waveshare_rp2040_zero` (`CMakeCache.txt`).
- Путь SDK при сборке: `/Users/kkk/pico-sdk`.

Точные коммиты SDK/TinyUSB и версия picotool для этой сборки не зафиксированы.
[Инструкция сборки и запуска](../../../firmware/rp2040/spi_bringup/README.md).

## Аппаратный результат

Выполнены `version` и `test 1000` на стенде RP2040-Zero ↔ FPGA `spi_bringup`
— **PASS**, 2026-09-07:

```text
TX: 01 00 00 00 00 00 00 00 00
RX: 00 47 42 46 43 01 00 00 00
OK: FPGA protocol 1.0, capabilities=0x0000

TEST: total=1000 pass=1000 fail=0 elapsed=8500 ms
PASS
```

Проверены ответ версии и серия из 1000 запросов — **PASS**, ошибок нет.
Хеши загруженных при испытании UF2 и JEDEC не зафиксированы; соответствие
испытанных байтов этому UF2 отдельно не сверялось. JEDEC в этот выпуск не входит.
Повторный тест после выключения/включения питания пройден — **PASS**.
Чтение, стирание и запись Flash не входят в протокол 1.0 и этим тестом не проверены.

[Журнал испытаний](../../../docs/TEST_PLAN.md#журнал-результатов).
