# План испытаний

## Уровни подтверждения

- **Проверено на аппаратуре:** результат наблюдался на указанном стенде.
- **Проверено в симуляции:** пройден описанный тест RTL или netlist; аппаратный
  результат этим не подтверждается.
- **Реализовано:** исходник или схема существует, но обязательный тест не
  пройден.
- **Запланировано:** зафиксирован только замысел.

Статус нельзя повышать до «проверено» без фиксации конфигурации, ревизии
артефакта, испытательного стенда и наблюдаемого результата.

## Журнал результатов

### 2026-09-09 — LSDj / MBC5: программные проверки — PASS

Добавлена конфигурация `game_mbc5` для ROM 1 МиБ и RAM 128 КиБ.
Icarus Verilog: 512 значений регистра ROM с зеркалированием на 64 банка,
границы ROM, два полных прохода записи/чтения 128 КиБ RAM, запрет RAM,
CS#, сброс, игнорируемые записи, направление данных — PASS.
В модели проверены precharge, CE#/WE# и setup данных F-RAM.
[Команды и параметры модели](../fpga/targets/game_mbc5/README.md#проверка-на-mac).

PROGRAMMER: F-RAM отключена при старте и SPI — PASS; регрессия блочного
RTL v3 на SPI 4 МГц — PASS. 11 host-тестов и тест C-кода RP2040 — PASS.
На модели с локальным `lsdj9_4_2.gb` пройдены два полных чтения 4 МиБ,
стирание, запись и побайтовое сравнение 1 МиБ для Top/Bottom Boot.
Данные за пределами ROM сохранены. Это программный тест, аппаратные
файлы дампов и измерения скорости LSDj не получены.
Исправлено подключение A10 F-RAM в схеме KiCad. Свежая сетка соединений:
66 назначений GAME, 40 назначений PROGRAMMER и все выводы F-RAM — PASS.
Новые GAME/PROGRAMMER ещё не собраны в Diamond и не проверены на стенде.

### 2026-09-06 — исправление стартового сброса: PASS

- Схема после синтеза воспроизвела причину кода 7: старт `state=0xFF`, затем
  `ST_FAIL`. В LSE были предупреждения об игнорировании значений из `initial`.
- Добавлена цепочка из четырёх примитивов `FD1S3AX`, удерживающая оба автомата
  в явном синхронном сбросе при старте. `mx29_bus` получил вход `reset`.
- Все восемь RTL-сценариев прошли; отдельная симуляция схемы после синтеза
  подтвердила корректный старт с неактивной шиной Flash.
- На стенде после новой прошивки зафиксированы **pin 82 HIGH, pin 83 LOW — PASS**.
  Пройдены ID, проверка начал стёртых секторов, 16 шаблонов данных,
  опорный адрес и 22 адресные пробы. Полное покрытие 4 МБ этим не подтверждается.
- Оба успешных запуска выполнены со сборкой от 23:23:22.
  JEDEC и контрольные суммы — в
  [flash-bringup-pass](../releases/fpga/flash-bringup-pass/).
- После полного выключения и включения питания PASS повторился.
  Зафиксированы два успешных запуска: после прошивки и после одного цикла
  выключения/включения. Отдельный выпуск с исходниками, отчётами и SHA-256 создан.

### 2026-09-08 — game_rom: аппаратная проверка — PASS

Стенд: Game Boy → преобразователи уровней → FPGA → внешняя Flash,
режим `game_rom`, ROM ONLY 32 КиБ. Игра загружается и работает стабильно —
**PASS**. Проверенная сборка: [RomEmu_game_rom.jed](../releases/fpga/game_rom/RomEmu_game_rom.jed),
отчёты Diamond сохранены в [releases/fpga/game_rom](../releases/fpga/game_rom/).

### 2026-09-08 — game_rom: RTL-проверка

Добавлен асинхронный ROM ONLY 32 КиБ из внешней Flash. Выполнены два
прогона Icarus Verilog: со случайным содержимым и с записанным
`flash-test-20260907-232422/rom.gb` — **PASS**, по 33 793 чтения.
Проверены все адреса ROM, освобождение шины в `8000–FFFF`, границы,
смена адреса при активном RD#, отдельные циклы чтения и изоляция записей CPU.
WE# Flash остаётся HIGH. Модель включает 70 нс доступа Flash и 10 нс
преобразователя данных; post-route задержки FPGA не моделировались.
Проверены 60 уникальных назначений выводов LPF, покрытие портов и пути LDF.
Сборка Diamond и аппаратный запуск ещё не выполнены.
[Конфигурация и инструкция](../fpga/targets/game_rom/README.md).

### 2026-09-07 — SPI: симуляция и сборка FPGA

Перенесено из README конфигурации; при актуализации документации тесты заново
не запускались. RTL и netlist — PASS; Map — без ошибок и предупреждений,
39/640 SLICE; timing setup/hold — 0 ошибок на 2,08 МГц.
Предупреждения LSE относятся к постоянному биту ответа и отсутствию
генерируемого LPF (Map получает отдельный LPF проекта); предупреждения
netlist-симуляции — к незадействованным входам примитивов.
Команды воспроизведения и охват тестов — в
[README конфигурации](../fpga/targets/spi_bringup/README.md#проверка).
Аппаратный результат зафиксирован следующей записью.

### 2026-09-07 — аппаратный SPI bring-up: PASS

Выполнены `version` и `test 1000` на стенде RP2040-Zero ↔ FPGA
с конфигурациями `spi_bringup` — **PASS**:

```text
TX: 01 00 00 00 00 00 00 00 00
RX: 00 47 42 46 43 01 00 00 00
OK: FPGA protocol 1.0, capabilities=0x0000

TEST: total=1000 pass=1000 fail=0 elapsed=8500 ms
PASS
```

Проверены ответ версии 1.0 и 1000 обменов через USB CDC / SPI — **PASS**, ошибок нет.
Последняя локальная сборка RP2040 сохранена в [выпуске](../releases/rp2040/spi_bringup/).
Рабочий FPGA JEDEC и отчёты сохранены в
[выпуске spi_bringup с SHA-256](../releases/fpga/spi_bringup/).
Повторный тест после выключения/включения питания пройден — **PASS**.
Команды памяти и полный тракт работы с Flash этим тестом не проверялись.


## Сверка LPF

Сверка выполняется для каждой конфигурации отдельно с учётом
[приоритета источников](README.md#приоритет-источников):

1. После синтеза открыть в Diamond `Spreadsheet View -> Port Assignments`.
   Diamond формирует полный список портов верхнего уровня автоматически;
   поддерживать отдельный список вручную не нужно.
2. Убедиться, что для каждого аппаратного порта в этой таблице заданы правильный
   вывод FPGA и электрический стандарт. В LPF им соответствуют записи
   `LOCATE COMP ... SITE ...` и `IOBUF`.
3. В схеме `hardware/cartridge/cartridge.kicad_sch` найти FPGA `U5` и для
   каждого порта сопоставить имя цепи и номер вывода с `SITE` в LPF. Шины нужно
   проверять поразрядно: например, `gb_a[0]` нельзя считать проверенным по
   совпадению только имени шины `gb_a`.
4. Проверить, что выбранные в Diamond устройство и корпус совпадают со схемой:
   `LCMXO2-1200HC-4TG100C`.
5. Убедиться, что в LPF нет ограничений для отсутствующих портов, а среди
   аппаратных портов `top` нет портов без назначения вывода. Предупреждения
   Diamond о выводах и стандартах ввода-вывода должны быть разобраны до
   программирования устройства.
6. После сборки сохранить отчёт о назначении выводов и сравнить его с LPF;
   результат сверки зафиксировать вместе с именем конфигурации и исходным
   коммитом.

Изменять схему только для устранения расхождения с LPF нельзя. Если найдено
расхождение, сначала нужно установить ожидаемое соединение по правилам
приоритета источников, а затем исправить зависимый артефакт.

## Проверка FPGA

Для каждой конфигурации сборки:

1. выполнить синтез;
2. выполнить описанную выше сверку LPF;
3. выполнить отображение логики, размещение, трассировку и генерацию потока
   конфигурации;
4. сохранить количество ошибок и предупреждений и отчёт временного анализа;
5. для асинхронных шин проверить заданные максимальные комбинационные задержки,
   а не только общий результат временного анализа.

Симуляция контроллера Flash должна покрывать чтение, последовательности команд,
тайм-аут, возврат в обычный режим чтения и невозможность одновременного
управления шиной несколькими устройствами.

## Проверка аппаратной части

До подачи питания:

- выполнить ERC схемы и DRC PCB;
- проверить сопротивление между питанием и землёй и целостность собранных цепей;
- проверить неактивные состояния Flash `CE#/OE#/WE#`, F-RAM `CE#/OE#/WE#` и
  `OE/DIR` преобразователей уровней;
- убедиться, что USB и Game Boy не могут питать друг друга обратным током.

## Последовательность первичной проверки

Этапы интеграции и критерии приёмки перечислены в [ROADMAP.md](ROADMAP.md).
Отдельная процедура первого обмена — в [SPI_BRINGUP.md](SPI_BRINGUP.md#аппаратная-приёмка).

## Данные выпуска

Требования к составу выпуска и проверке его целостности — в
[releases/README.md](../releases/README.md).
